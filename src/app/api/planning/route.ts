// Legacy compatibility endpoint.
// The canonical implementation lives under /api/agenda/planning and
// enforces authentication, feature access, ownership, and RLS.
export { GET, POST } from '@/app/api/agenda/planning/route'
