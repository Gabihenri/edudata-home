# Public Supabase configuration (safe to expose only as project URL/public key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only. Never expose this value to the browser and never commit a real key.
SUPABASE_SERVICE_ROLE_KEY=
